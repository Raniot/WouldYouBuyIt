name: would_you_buy_it
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  http: ^1.0.0
  photo_view: ^0.14.0
  intl: ^0.18.1
  fluttertoast: ^8.2.2

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter

flutter_icons:
  image_path: "icon/houseIcon.png" 
  android: true
  ios: true

flutter:
  uses-material-design: true
